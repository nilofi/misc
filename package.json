{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean",
    "prepare": "husky install",
    "preinstall": "npx only-allow pnpm"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^18.2.0",
    "@commitlint/config-conventional": "^18.1.0",
    "@xenon.js/prettier-config": "^1.0.2",
    "cross-env": "^7.0.3",
    "husky": "^8.0.3",
    "only-allow": "^1.2.1",
    "prettier": "3.0.3",
    "rimraf": "^5.0.5",
    "turbo": "^1.10.16",
    "typescript": "^5.2.2"
  }
}
